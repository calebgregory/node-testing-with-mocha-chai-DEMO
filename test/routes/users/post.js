'use strict'

const { expect } = require('../../setup')

describe('routes', () => {
  describe('/users', () => {
    describe('POST', () => {
      it('returns json')
      it('returns a 200')
    })
  })
})
