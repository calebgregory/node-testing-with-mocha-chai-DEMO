'use strict'

const { expect } = require('../../setup')

describe('routes', () => {
  describe('/users', () => {
    describe('GET', () => {
      it('returns json')
      it('returns a 200')
    })
  })
})
